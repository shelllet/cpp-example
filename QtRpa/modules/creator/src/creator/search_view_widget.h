#pragma once
#include <qtreeview.h>
#include "model.hpp"
namespace shelllet::creator {
	class ActivitiesSearchViewWidget : public QTreeView
	{
	public:
		ActivitiesSearchViewWidget(QWidget* parent = nullptr);
		~ActivitiesSearchViewWidget();

	public:
	private:
	private:
		
	protected:
	};
}
